package com.newday

object Constants {
  val INPUT_FILE = "input.file"
  val OUTPUT_FILE = "output.file"
  val READ_FORMAT = "read.format"
  val WRITE_FORMAT = "write.format"
  val APP_NAME = "application"
  val MASTER = "master"
}
